---
phase: quick
plan: 260331-lq1
type: execute
wave: 1
depends_on: []
files_modified:
  - student_finetune/prepare.py
  - student_finetune/train.py
  - student_finetune/build_caches.py
  - student_finetune/tests/test_train.py
  - student_finetune/tests/test_infrastructure.py
  - README.md
autonomous: true
requirements: [RESTRUCTURE]

must_haves:
  truths:
    - "student_finetune/ contains prepare.py, train.py, build_caches.py, tests/"
    - "prepare.py can be imported from student_finetune/ directory"
    - "All relative paths resolve correctly from student_finetune/ working directory"
    - "Tests pass when run from student_finetune/"
    - "README.md explains the multi-subfolder structure"
  artifacts:
    - path: "student_finetune/prepare.py"
      provides: "Relocated prepare module with fixed relative paths"
    - path: "student_finetune/train.py"
      provides: "Relocated train module"
    - path: "student_finetune/build_caches.py"
      provides: "Relocated cache builder with fixed relative paths"
    - path: "README.md"
      provides: "Project structure documentation"
  key_links:
    - from: "student_finetune/train.py"
      to: "student_finetune/prepare.py"
      via: "from prepare import (...)"
      pattern: "from prepare import"
    - from: "student_finetune/prepare.py"
      to: "../dino_finetune/output/best_adapter"
      via: "relative path in DINOv3FTTeacher and TEACHER_REGISTRY"
      pattern: "\\.\\./dino_finetune/output/best_adapter"
---

<objective>
Restructure autoresearch project by moving student distillation code (prepare.py, train.py, build_caches.py, tests/) into a student_finetune/ subfolder, fixing all relative paths, and creating a root README.md.

Purpose: Establish clean multi-subfolder pattern (dino_finetune/, student_finetune/) where each research topic is self-contained.
Output: Working student_finetune/ subdirectory with all tests passing, plus root README.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@prepare.py
@train.py
@build_caches.py
@tests/test_infrastructure.py
@tests/test_train.py
@tests/test_prepare.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Git-move files into student_finetune/</name>
  <files>student_finetune/prepare.py, student_finetune/train.py, student_finetune/build_caches.py, student_finetune/tests/</files>
  <action>
    Create the student_finetune/ directory and git-move all files:

    ```bash
    mkdir -p student_finetune
    git mv prepare.py student_finetune/prepare.py
    git mv train.py student_finetune/train.py
    git mv build_caches.py student_finetune/build_caches.py
    git mv tests/ student_finetune/tests/
    ```

    Do NOT move: dino_finetune/, RADIO/, workspace/, pyproject.toml, .gitignore, or any other root files.
  </action>
  <verify>
    <automated>ls student_finetune/prepare.py student_finetune/train.py student_finetune/build_caches.py student_finetune/tests/test_train.py student_finetune/tests/test_prepare.py student_finetune/tests/test_infrastructure.py student_finetune/tests/conftest.py</automated>
  </verify>
  <done>All student distillation files are in student_finetune/ and tracked by git</done>
</task>

<task type="auto">
  <name>Task 2: Fix relative paths in prepare.py and build_caches.py</name>
  <files>student_finetune/prepare.py, student_finetune/build_caches.py</files>
  <action>
    In student_finetune/prepare.py, fix ALL relative paths that reference sibling directories. These paths are now one level deeper, so they need "../" prefix:

    1. Line 264 (DINOv3FTTeacher.__init__ default): `"dino_finetune/output/best_adapter"` -> `"../dino_finetune/output/best_adapter"`
    2. Line 374 (torch.hub.load): `"RADIO"` -> `"../RADIO"` (the source="local" hub path)
    3. Line 743 (TEACHER_REGISTRY dinov2 cache_dir): `"workspace/output/teacher_cache/dinov2"` -> `"../workspace/output/teacher_cache/dinov2"`
    4. Line 749 (TEACHER_REGISTRY dinov3_ft cache_dir): `"workspace/output/teacher_cache/dinov3_ft"` -> `"../workspace/output/teacher_cache/dinov3_ft"`
    5. Line 750 (TEACHER_REGISTRY dinov3_ft init_kwargs adapter_path): `"dino_finetune/output/best_adapter"` -> `"../dino_finetune/output/best_adapter"`
    6. Line 755 (TEACHER_REGISTRY radio_so400m cache_dir): `"workspace/output/teacher_cache/radio_so400m"` -> `"../workspace/output/teacher_cache/radio_so400m"`
    7. Line 761 (TEACHER_REGISTRY radio_h cache_dir): `"workspace/output/teacher_cache/radio_h"` -> `"../workspace/output/teacher_cache/radio_h"`

    DO NOT change:
    - DEFAULT_TEACHER_CACHE_DIR (line 57) — it is already an absolute path starting with /data/
    - Any /data/mnt/... absolute paths (TRAIN_DIR, VAL_DIR, ARCFACE_DIR, REID paths)

    In student_finetune/build_caches.py, fix:
    1. Line 107: `f"workspace/output/teacher_cache/{radio_name}"` -> `f"../workspace/output/teacher_cache/{radio_name}"`

    The docstring at line 581 mentioning "workspace/output/teacher_cache" is just a comment — update it for accuracy but it is not functional.
  </action>
  <verify>
    <automated>cd student_finetune && python -c "from prepare import TEACHER_REGISTRY; assert '../dino_finetune' in TEACHER_REGISTRY['dinov3_ft']['init_kwargs']['adapter_path']; assert '../workspace' in TEACHER_REGISTRY['dinov2']['cache_dir']; print('All paths fixed')"</automated>
  </verify>
  <done>All relative paths in prepare.py and build_caches.py resolve correctly from student_finetune/ working directory</done>
</task>

<task type="auto">
  <name>Task 3: Fix test file path references</name>
  <files>student_finetune/tests/test_infrastructure.py, student_finetune/tests/test_train.py</files>
  <action>
    In student_finetune/tests/test_infrastructure.py, fix hardcoded open() calls that use relative paths. These tests will now run from student_finetune/ as CWD:

    1. Lines 137,144,149,154,160,165,187,193,198: `open("train.py")` — these are fine, they resolve to student_finetune/train.py when CWD is student_finetune/
    2. Line 172: `open("prepare.py")` — fine for same reason
    3. Lines 209,213,217: `open(".gitignore")` -> `open("../.gitignore")` — .gitignore is at project root, not in student_finetune/

    In student_finetune/tests/test_train.py:
    1. Line 203: `Path(__file__).parent.parent / "pyproject.toml"` -> `Path(__file__).parent.parent.parent / "pyproject.toml"` — pyproject.toml is at project root. After move, __file__ is student_finetune/tests/test_train.py, so parent.parent = student_finetune/, parent.parent.parent = project root.

    In student_finetune/tests/test_prepare.py: Review all assertions. The imports (`import prepare`, `import train` via inspect) work because tests run from student_finetune/ CWD. No changes needed unless path-dependent assertions exist (there are none — all assertions check attributes/functions, not file paths, except the DEFAULT_TEACHER_CACHE_DIR check on line 137 which uses an absolute path constant anyway).
  </action>
  <verify>
    <automated>cd student_finetune && python -c "
import ast
# Check .gitignore path was updated
src = open('tests/test_infrastructure.py').read()
assert '../.gitignore' in src, '.gitignore path not updated'
assert 'open(\".gitignore\")' not in src, 'Old .gitignore path still present'
# Check pyproject path was updated
src2 = open('tests/test_train.py').read()
assert 'parent.parent.parent' in src2, 'pyproject.toml path not updated'
print('All test paths fixed')
"</automated>
  </verify>
  <done>Test files reference correct relative paths from their new location</done>
</task>

<task type="auto">
  <name>Task 4: Create root README.md and verify imports</name>
  <files>README.md</files>
  <action>
    Create README.md at project root explaining the multi-subfolder autoresearch structure:

    - Title: autoresearch
    - Brief description: Autonomous ML research platform where AI agents explore model configurations
    - Structure section explaining each subfolder:
      - dino_finetune/ — DINOv3 ViT-H+ fine-tuning with LoRA for teacher model
      - student_finetune/ — Student model distillation (LCNet) from multiple teachers
      - RADIO/ — Local clone of C-RADIO model repository (used as torch.hub source)
      - workspace/ — Runtime artifacts (caches, outputs, results)
    - Usage section: How to run each subfolder (cd into it, python train.py or python train_dino.py)
    - Note: Each subfolder is designed to be run with its own directory as CWD
    - How to add new research topics: create new subfolder following the pattern

    After writing README.md, run the full import verification:

    ```bash
    cd student_finetune && python -c "from prepare import TEACHER_REGISTRY; print('prepare OK')"
    cd student_finetune && python -c "import train; print('train OK')"
    cd student_finetune && python -c "import build_caches; print('build_caches OK')"
    ```
  </action>
  <verify>
    <automated>test -f README.md && cd student_finetune && python -c "from prepare import TEACHER_REGISTRY; print('prepare OK')" && python -c "import train; print('train OK')"</automated>
  </verify>
  <done>README.md exists at project root. All three Python modules import successfully from student_finetune/ directory.</done>
</task>

</tasks>

<verification>
1. All files moved: `ls student_finetune/{prepare,train,build_caches}.py student_finetune/tests/`
2. Imports work: `cd student_finetune && python -c "from prepare import TEACHER_REGISTRY; print(TEACHER_REGISTRY.keys())"`
3. Relative paths correct: `cd student_finetune && python -c "from prepare import TEACHER_REGISTRY; [print(f'{k}: {v.get(\"cache_dir\",\"\")}') for k,v in TEACHER_REGISTRY.items()]"` — all cache_dir values start with "../"
4. Tests pass: `cd student_finetune && python -m pytest tests/ -x -q` (may need GPU-dependent tests skipped, but import/schema tests should pass)
5. README exists: `cat README.md`
</verification>

<success_criteria>
- student_finetune/ contains prepare.py, train.py, build_caches.py, tests/
- No prepare.py, train.py, build_caches.py, or tests/ remain at project root
- All relative paths in prepare.py point to ../dino_finetune/ and ../workspace/
- RADIO hub.load path is ../RADIO
- test_infrastructure.py references ../.gitignore
- test_train.py uses parent.parent.parent for pyproject.toml
- All three modules import successfully from student_finetune/ CWD
- README.md explains project structure
</success_criteria>

<output>
After completion, create `.planning/quick/260331-lq1-restructure-project-move-student-distill/260331-lq1-SUMMARY.md`
</output>
